<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <title>notes App</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div id="form" class="form p-10 text-white p-10">
            <form action="/create" method="post">
                <input name="title" class="block w-full rounded-md outline-none px-10  bg-zinc-700" type="text" placeholder="Title goes here">
                <textarea name="details" class="block w-full resize-none rounded-md outline-none px-10 mt-5 bg-zinc-700" placeholder="Write your task"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit">
            </form>
        </div>
        <div class="tasks  flex gap-3 flex-wrap p-10">
        <% if(files.length > 0) {%>
            <% files.forEach((val)=> { %>
                <div class="task min-w-72  px-3 py-4 rounded-md bg-zinc-800 ">
                    <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                    <div class="flex w-full justify-between items-center mt-4 ">
                        <a class="text-blue-500 inline-block " href="/files/<%= val %>">Read More</a>
                        <a class="text-zinc-500" href="/edit/<%= val %>">Edit File Name</a>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
        <h3 class="text-zinc-500 ">no-task yet</h3>
        <% } %>          
        </div>
    </div>
</body>
</html> 

  -->
<!-- new ui under this  -->
<!DOCTYPE html>
<html class="dark" lang="en">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>NoteIt - Modern Notes App</title>
    <link href="https://fonts.googleapis.com" rel="preconnect" />
    <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&amp;display=swap"
        rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <script>
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#137fec",
                        "background-light": "#f6f7f8",
                        "background-dark": "#101922",
                        "card-dark": "#182431",
                        "card-hover-dark": "#1f2d3d"
                    },
                    fontFamily: {
                        "display": ["Inter", "sans-serif"]
                    },
                    borderRadius: {
                        "DEFAULT": "0.25rem",
                        "lg": "0.5rem",
                        "xl": "0.75rem",
                        "full": "9999px"
                    },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>

<body class="bg-background-dark font-display text-slate-200">
    <div class="flex flex-col min-h-screen">
        <main class="flex-grow container mx-auto px-4 sm:px-6 lg:px-8 py-8 md:py-12">
            <div class="max-w-2xl mx-auto">
                <div class="text-center mb-12">
                    <div class="flex items-center justify-center gap-4 mb-4">
                        <svg class="h-8 w-8 text-primary" fill="none" viewBox="0 0 48 48"
                            xmlns="http://www.w3.org/2000/svg">
                            <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                        </svg>
                        <h1 class="text-3xl font-bold text-white">NoteIt</h1>
                    </div>
                    <p class="text-slate-400">Your minimal and modern notes companion.</p>
                </div>
                <div class="mb-12">
                    <h2 class="text-2xl md:text-3xl font-bold text-white mb-6">Create a New Note</h2>
                    <form action="/create" method="post" class="space-y-4">
                        <div>
                            <label class="sr-only" for="note-title">Note Title</label>
                            <input
                                class="w-full bg-slate-800/50 border border-slate-700 focus:ring-2 focus:ring-primary focus:border-transparent rounded-lg p-4 placeholder-slate-500 text-white"
                                id="note-title" name="title" placeholder="Note Title" type="text" />
                        </div>
                        <div>
                            <label class="sr-only" for="note-content">Note Content</label>
                            <textarea
                                class="w-full bg-slate-800/50 border border-slate-700 focus:ring-2 focus:ring-primary focus:border-transparent rounded-lg p-4 placeholder-slate-500 text-white resize-none"
                                id="note-content" name="details" placeholder="Jot down your thoughts..." rows="5"></textarea>
                        </div>
                        <div class="flex justify-end">
                            <button
                                class="inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-medium rounded-lg text-white bg-primary hover:bg-opacity-90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary dark:focus:ring-offset-background-dark transition-colors"
                                type="submit">
                                Create Note
                            </button>
                        </div>
                    </form>
                </div>
                <div>
                    <h2 class="text-2xl md:text-3xl font-bold text-white mb-6">My Notes</h2>
                    <% if(files.length > 0) { %> 
                        <% files.forEach((val)=> { %>
                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-2 gap-6 p-6">
                        <a class="group block bg-card-dark hover:bg-card-hover-dark p-6 rounded-xl transition-all duration-300 transform hover:-translate-y-1"
                            href="/files/<%= val %>">
                            <h3 class="font-semibold text-white mb-2 text-lg"><%= val %></h3>
                            <!-- <a class="text-blue-500 inline-block " href="/files/<%= val %>">Read More</a> -->
                            <button class="text-zinc-500 p-3 hover:text-blue-500" href="/edit/<%= val %>">Edit File Name</button> 
                        </a>
                    </div>
                    <% }) %>
                    <% } else { %>
                        <p class="text-slate-400">No notes available. Create a new note to get started!</p>
                    <% } %>
                </div>
            </div>
        </main>
    </div>

</body>

</html>