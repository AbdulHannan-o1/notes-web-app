<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <title>notes App</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div id="form" class="form p-10 text-white p-10">
            <h1 class="text-4xl tracking-tighter">
                Delete note Permanently
            </h1>
            <form action="/deleteFile/<%= filename %>" method="post">
                <p>Are you sure to delete this note: <%= filename %> Permanently</p>
                <button class="bg-red-700 px-4 py-2 rounded" type="submit">Delete Now</button>
            </form>
        </div>
    </div>
</body>
</html> 